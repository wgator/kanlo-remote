{
  "name": "kanlo-remote",
  "version": "0.0.1",
  "description": "A proxy-server that allows work on kanlo projects with local files",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rafael Correia",
  "license": "MIT",
  "dependencies": {
    "http-proxy": "~1.0.2"
  }
}
